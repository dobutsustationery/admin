# CSS Critique

## Scope and Method

- Searched all `*.svelte` files under `src/` for Tailwind-style utility classes in `class="..."` attributes.
- Flagged class attributes containing utility tokens (spacing, color, layout, state variants like `hover:`/`md:`).
- Searched repo (excluding `.git`) for `tailwind` and `taildwind` references.

## Tailwind/Taildwind References

- No `tailwind` or `taildwind` references found in tracked code/docs outside VCS metadata.

## Tailwind-Style Utility Class Usages in Svelte Components

| File                                            | Line | Class Attribute                                                                                                                                                                                                                          | Why this is an issue                                              | Scoped Svelte Style Direction                                                                                        |
| ----------------------------------------------- | ---: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/lib/components/Celebration.svelte`         |   47 | `text-4xl font-bold text-blue-600 mb-4 animate-bounce`                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/Celebration.svelte`         |   50 | `text-xl text-gray-700`                                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ListingVariantModal.svelte` |  336 | `flex items-center justify-between mb-2`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ListingVariantModal.svelte` |  358 | `flex items-center gap-3`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ListingVariantModal.svelte` |  379 | `flex items-center gap-2`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ListingVariantModal.svelte` |  425 | `flex items-center gap-3`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  128 | `font-bold text-lg`                                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  130 | `text-gray-500 hover:text-gray-700`                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  151 | `flex items-center gap-4`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  152 | `text-sm font-bold text-gray-500 w-16`                                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  155 | `flex-1 h-2 bg-gray-200 rounded-lg appearance-none cursor-pointer`                                                                                                                                                                       | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  162 | `text-xs font-mono w-12 text-right`                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  167 | `flex justify-end gap-2`                                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  169 | `px-4 py-2 text-gray-700 font-medium hover:bg-gray-200 rounded`                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ManualCropModal.svelte`     |  173 | `px-6 py-2 bg-indigo-600 text-white font-bold rounded hover:bg-indigo-700 shadow-sm`                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ProgressBar.svelte`         |    8 | `px-4 py-2`                                                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ProgressBar.svelte`         |    9 | `flex justify-between text-sm text-gray-600 mb-1`                                                                                                                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ProgressBar.svelte`         |   13 | `w-full bg-gray-200 rounded-full h-2 overflow-hidden`                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/ProgressBar.svelte`         |   15 | `{colorClass} h-2 rounded-full transition-all duration-300`                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/SecureImage.svelte`         |  267 | `{className} bg-gray-200 animate-pulse flex items-center justify-center`                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/SecureImage.svelte`         |  269 | `text-xs text-gray-400`                                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/SecureImage.svelte`         |  273 | `{className} bg-red-100 flex items-center justify-center p-2 text-center`                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/lib/components/SecureImage.svelte`         |  276 | `text-xs text-red-500 font-medium overflow-hidden text-ellipsis px-1`                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/audit/+page.svelte`                 |  257 | `flex items-center gap-1`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/audit/+page.svelte`                 |  264 | `flex items-center gap-1 export-btn`                                                                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2269 | `flex justify-between items-center mb-4`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2272 | `flex items-center gap-2 text-sm cursor-pointer select-none`                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2292 | `p-8 text-center text-gray-500 bg-gray-50 rounded border border-dashed border-gray-300 mt-4`                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2295 | `text-xs mt-2`                                                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2298 | `text-xs block mt-1 bg-white p-1 rounded border`                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2301 | `text-xs mt-2 text-gray-400`                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2309 | `modal-actions flex justify-end gap-2`                                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listing-detail/+page.svelte`        | 2346 | `modal-actions flex justify-end gap-2`                                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  725 | `container mx-auto p-6`                                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  726 | `text-3xl font-bold mb-6`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  732 | `fixed inset-0 flex items-center justify-center z-50 pointer-events-none`                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  734 | `pointer-events-auto mt-64`                                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  737 | `bg-white text-blue-600 px-8 py-4 rounded-full font-bold text-xl shadow-2xl hover:scale-105 transition-transform border-4 border-blue-100`                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  747 | `bg-yellow-50 p-8 rounded border border-yellow-200 text-center`                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  748 | `text-xl font-bold mb-4 text-yellow-800`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  751 | `mb-6 text-yellow-700`                                                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  756 | `bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700 font-bold shadow-lg`                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  764 | `h-[calc(100vh-100px)] -mx-6 flex flex-col`                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  767 | `flex justify-between items-center px-6 py-2 bg-white border-b gap-4`                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  770 | `flex items-center justify-between mb-1`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  771 | `text-xl font-bold`                                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  774 | `text-sm text-gray-500`                                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  778 | `w-full bg-gray-200 rounded-full h-2.5`                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  780 | `bg-blue-600 h-2.5 rounded-full transition-all duration-500`                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  786 | `btn-save start-review-btn ml-4`                                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  809 | `bg-white p-8 rounded shadow text-center`                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  810 | `text-2xl font-bold mb-4`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  811 | `mb-6 text-gray-600`                                                                                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  816 | `bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700 font-bold`                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  823 | `bg-gray-50 p-8 rounded border border-dashed border-gray-300 text-center`                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  825 | `text-xl font-medium mb-4 text-gray-500`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  829 | `mb-4 text-orange-600 bg-orange-50 p-4 rounded inline-block text-left max-w-lg`                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  836 | `underline font-bold`                                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  844 | `flex flex-col items-center justify-center gap-4 w-full`                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  849 | `w-full max-w-md`                                                                                                                                                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  858 | `mt-4 p-4 bg-orange-50 border border-orange-200 rounded-md`                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  860 | `text-orange-800 text-sm mb-3`                                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  872 | `bg-orange-600 text-white px-4 py-2 rounded font-bold hover:bg-orange-700 transition shadow-sm`                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  880 | `flex items-center justify-center gap-4`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  883 | `bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700 font-bold shadow-lg transition-transform hover:scale-105`                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/listings/create/+page.svelte`       |  964 | `mb-4 text-sm text-gray-500`                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/order-import/+page.svelte`          | 1003 | `font-bold mb-2`                                                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/order-import/+page.svelte`          | 1007 | `text-sm font-semibold mb-1`                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/order-import/+page.svelte`          | 1008 | `flex flex-col gap-2`                                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photo-history/+page.svelte`         |  693 | `flex justify-between items-center mb-6`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photo-history/+page.svelte`         |  697 | `flex items-center gap-2 text-xs text-indigo-600 font-medium animate-pulse`                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photo-history/+page.svelte`         |  700 | `w-3 h-3 border-2 border-indigo-600 border-t-transparent rounded-full animate-spin`                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1217 | `p-8 max-w-6xl mx-auto relative`                                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1218 | `flex justify-between items-center mb-8`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1220 | `text-3xl font-bold text-gray-800`                                                                                                                                                                                                       | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1221 | `mt-2 flex items-center gap-3`                                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1225 | `inline-flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-sm font-medium bg-green-100 text-green-800`                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1228 | `w-2 h-2 rounded-full bg-green-600`                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1233 | `inline-flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-sm font-medium bg-gray-100 text-gray-800`                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1235 | `w-2 h-2 rounded-full bg-gray-400`                                                                                                                                                                                                       | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1243 | `text-sm text-blue-600 hover:text-blue-800 hover:underline font-medium`                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1253 | `bg-white p-6 rounded-lg shadow-md min-h-[400px]`                                                                                                                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1257 | `p-4 bg-red-50 text-red-700 rounded mb-4 mt-6`                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1264 | `bg-slate-50 relative mt-8`                                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1268 | `flex justify-between items-center px-4 pt-4 pb-2`                                                                                                                                                                                       | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1269 | `flex gap-2`                                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1272 | `bg-red-100 text-red-700 px-3 py-2 rounded-md font-medium hover:bg-red-200 transition text-sm mr-2`                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1281 | `bg-green-600 text-white px-4 py-2 rounded-md font-medium hover:bg-green-700 transition disabled:opacity-50 flex items-center gap-2 text-sm`                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1285 | `animate-spin h-3 w-3 border-2 border-white border-t-transparent rounded-full`                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1296 | `bg-blue-600 text-white px-4 py-2 rounded-md font-medium hover:bg-blue-700 transition disabled:opacity-50 flex items-center gap-2 text-sm`                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1300 | `animate-spin h-3 w-3 border-2 border-white border-t-transparent rounded-full`                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1309 | `flex gap-2`                                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1314 | `bg-teal-600 text-white px-4 py-2 rounded-md font-medium hover:bg-teal-700 transition disabled:opacity-50 flex items-center gap-2 text-sm`                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1323 | `bg-gray-100 text-gray-700 border border-gray-300 px-4 py-2 rounded-md font-medium hover:bg-gray-200 transition disabled:opacity-50 flex items-center gap-2 text-sm`                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1334 | `flex flex-row flex-wrap gap-4 mt-2 mb-6 p-4 min-h-[160px]`                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1342 | `bg-white rounded-lg overflow-hidden border border-gray-200 shadow-sm relative group cursor-pointer hover:ring-2 hover:ring-indigo-500 transition-all`                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1373 | `absolute inset-0 bg-blue-600/40 flex items-center justify-center z-20 backdrop-blur-[1px]`                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1376 | `text-white text-xs font-bold flex flex-col items-center drop-shadow-md`                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1379 | `animate-spin h-5 w-5 border-2 border-white border-t-transparent rounded-full mb-1`                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1381 | `uppercase tracking-wider text-[10px]`                                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1388 | `absolute top-1 right-1 bg-yellow-400 text-yellow-900 text-[10px] font-bold px-1.5 py-0.5 rounded shadow-sm z-20 border border-yellow-500`                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1399 | `absolute inset-0 bg-black/30 flex items-center justify-center`                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1402 | `animate-spin h-6 w-6 border-2 border-white border-t-transparent rounded-full`                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1407 | `absolute inset-0 bg-red-500/30 flex items-center justify-center`                                                                                                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1410 | `text-white font-bold text-xl`                                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1417 | `w-full text-center py-10 text-gray-500`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1422 | `w-full text-center py-10 text-gray-500 italic`                                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1426 | `w-full text-center py-10 text-gray-400 italic`                                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1436 | `bg-white p-6 rounded-lg shadow-md mt-8 border-t-4 border-teal-500`                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1439 | `flex justify-between items-center mb-4`                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1440 | `text-xl font-bold text-gray-800`                                                                                                                                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1441 | `flex gap-4 items-center`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1443 | `flex items-center gap-2 text-sm text-gray-600 bg-gray-50 px-3 py-1.5 rounded border border-gray-200 cursor-pointer hover:bg-gray-100 transition`                                                                                        | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1448 | `rounded text-teal-600 focus:ring-teal-500`                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1455 | `bg-slate-200 text-slate-700 px-3 py-2 rounded-md font-bold hover:bg-slate-300 transition text-sm flex items-center gap-2`                                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1464 | `bg-indigo-600 text-white px-4 py-2 rounded-md font-bold hover:bg-indigo-700 transition disabled:opacity-50 text-sm flex items-center gap-2 shadow-sm`                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1469 | `animate-spin h-3 w-3 border-2 border-white border-t-transparent rounded-full`                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1476 | `bg-green-600 text-white px-4 py-2 rounded-md font-bold hover:bg-green-700 transition text-sm flex items-center gap-2 shadow-sm`                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1484 | `border border-gray-300 rounded-lg overflow-hidden bg-white shadow-sm`                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1488 | `flex flex-row bg-slate-200 border-b-2 border-slate-300 text-slate-700`                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1492 | `w-48 flex-none p-4 font-bold text-center uppercase tracking-wide text-sm`                                                                                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1498 | `flex-1 p-4 font-bold text-sm uppercase tracking-wide`                                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1513 | `flex flex-row categorized-row group`                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1526 | `w-48 flex-none p-4 font-mono text-lg font-medium text-teal-700 break-all bg-gray-50/50 group-hover:bg-transparent transition-colors z-10`                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1552 | `flex-1 p-4 min-w-0 relative`                                                                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1563 | `absolute -top-3 right-4 bg-yellow-100 hover:bg-yellow-200 text-yellow-800 border border-yellow-300 rounded-full px-3 py-1 text-xs font-bold shadow-sm z-50 flex items-center gap-1 cursor-pointer transition-transform hover:scale-105` | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1574 | `flex flex-row flex-wrap gap-4 mt-6 mb-6 p-4`                                                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1579 | `bg-white rounded-lg overflow-hidden border border-gray-200 shadow-sm relative group/item cursor-pointer hover:ring-2 hover:ring-indigo-500 transition-all`                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1606 | `absolute bottom-0 left-0 right-0 bg-black/60 text-white text-[9px] p-0.5 truncate opacity-0 group-hover/item:opacity-100 transition-opacity`                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1613 | `absolute top-1 right-1 flex flex-col gap-0.5`                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1617 | `w-1.5 h-1.5 rounded-full bg-green-500 shadow-sm border border-white`                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1623 | `w-1.5 h-1.5 rounded-full bg-indigo-500 shadow-sm border border-white`                                                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1629 | `w-1.5 h-1.5 rounded-full bg-pink-500 shadow-sm border border-white`                                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1638 | `absolute inset-0 bg-black/30 flex items-center justify-center`                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1641 | `animate-spin h-5 w-5 border-2 border-white border-t-transparent rounded-full`                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/photos/+page.svelte`                | 1646 | `absolute inset-0 bg-red-500/30 flex items-center justify-center font-bold text-white text-xs`                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  978 | `panel migration-panel mb-4 bg-blue-50 border-blue-200`                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  979 | `flex justify-between items-center`                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  981 | `text-blue-800 m-0`                                                                                                                                                                                                                      | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  982 | `text-sm text-blue-600 mt-1`                                                                                                                                                                                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  987 | `flex items-center gap-3`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        |  989 | `text-xs text-blue-600 animate-pulse`                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1002 | `w-full bg-blue-200 h-1 mt-3 rounded overflow-hidden`                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1005 | `h-full bg-blue-600 animate-progress`                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1116 | `mb-4 p-4 bg-gray-50 rounded border border-gray-200`                                                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1118 | `flex items-center gap-2 text-sm bg-white px-3 py-1 rounded shadow-sm border`                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1127 | `flex items-center gap-2 text-sm bg-white px-3 py-1 rounded shadow-sm border`                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1133 | `flex items-center gap-2 text-sm bg-white px-3 py-1 rounded shadow-sm border`                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1139 | `flex items-center gap-2 text-sm bg-white px-3 py-1 rounded shadow-sm border`                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1145 | `flex items-center gap-2 text-sm bg-white px-3 py-1 rounded shadow-sm border`                                                                                                                                                            | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1150 | `text-xs text-muted mt-1`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1256 | `message error mb-4`                                                                                                                                                                                                                     | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1263 | `text-xs font-mono mt-2 p-2 bg-red-100 rounded`                                                                                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1267 | `text-sm mt-2`                                                                                                                                                                                                                           | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1275 | `font-bold mb-2`                                                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1276 | `flex flex-col gap-2`                                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1344 | `text-gray-400 font-normal`                                                                                                                                                                                                              | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/shopify-import/+page.svelte`        | 1346 | `text-xs italic`                                                                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |   90 | `p-8 max-w-4xl mx-auto`                                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |   91 | `text-2xl font-bold mb-4`                                                                                                                                                                                                                | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |   95 | `bg-white p-4 rounded shadow space-y-4`                                                                                                                                                                                                  | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |   97 | `block font-semibold mb-1`                                                                                                                                                                                                               | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  103 | `block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100`                                             | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  110 | `px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 disabled:opacity-50`                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  118 | `p-2 bg-red-100 text-red-700 rounded`                                                                                                                                                                                                    | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  123 | `grid grid-cols-2 gap-4`                                                                                                                                                                                                                 | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  126 | `bg-gray-900 text-green-400 p-4 rounded font-mono text-xs h-[500px] overflow-auto whitespace-pre-wrap`                                                                                                                                   | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  133 | `bg-gray-100 p-4 rounded flex items-center justify-center min-h-[500px] border-2 border-dashed border-gray-300`                                                                                                                          | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |
| `src/routes/test-edit/+page.svelte`             |  143 | `animate-spin rounded-full h-12 w-12 border-b-2 border-gray-400`                                                                                                                                                                         | Utility-chain styling in markup couples structure + presentation. | Replace with semantic classes (e.g. `.panel`, `.status-pill`) and move declarations into this component’s `<style>`. |

## Recommended Refactor Pattern (apply to each row above)

1. Replace utility-heavy `class="..."` strings with semantic class names tied to component intent.
2. Move visual rules into local scoped `<style>` blocks in the same `.svelte` file.
3. Use Svelte class directives only for state (`class:is-loading`, `class:is-active`) and keep declarations in scoped CSS.
4. Consolidate repeated tokens (`flex items-center gap-2`, button color variants, badge styles) into reusable component-local CSS classes.
5. Preserve behavior variants (`hover`, `focus`, `disabled`) as scoped selectors (`.btn:hover`, `.btn:disabled`) instead of utility syntax.
